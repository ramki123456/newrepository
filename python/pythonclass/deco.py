############# Decorators ##################
# a decorator is a wrapper function which will declare on top of any python function.
# the decorator function name should preceded with ' @ 'symbol.
#a decorator function will take  the main function name as a input
# whenever a main function call then python will execute decorator function, based on the o/p of decorator the main function will execute.




#*** NOte : a decorator function will take  the main function name as a input.****
 
