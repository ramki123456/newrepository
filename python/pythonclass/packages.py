######## package ###################

# package is a python container(project folder) which can hold multiple floders and files.
# To convert a normal folder into package place __init__.py file inside the folder.
#complete project details should placed within a folder that folder is known as project folder.


     #syntax : 

from myfolder.test import get_even
print get_even(range(10))
